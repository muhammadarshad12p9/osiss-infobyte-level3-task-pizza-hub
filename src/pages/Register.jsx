import{useState}from'react';import{Link,useNavigate}from'react-router-dom';import{useAuth}from'../context/AuthContext';
export default function Register(){const[f,setF]=useState({name:'',email:'',password:''}),[msg,setMsg]=useState(''),[ok,setOk]=useState(false);const{register}=useAuth();const nav=useNavigate();function submit(e){e.preventDefault();if(f.password.length<6){setMsg('Password must be at least 6 characters.');return}const r=register(f.name,f.email,f.password);setMsg(r.message);setOk(r.ok);if(r.ok)setTimeout(()=>nav('/login'),800)}return <section className="card auth"><h1>Create account</h1><p>Register a new PizzaHub account.</p><form onSubmit={submit}>{['name','email','password'].map(k=><input key={k} type={k==='password'?'password':k==='email'?'email':'text'} placeholder={k[0].toUpperCase()+k.slice(1)} value={f[k]} onChange={e=>setF({...f,[k]:e.target.value})} required/>)}<button>Register</button></form>{msg&&<p className={ok?'success':'error'}>{msg}</p>}<p><Link to="/login">Back to login</Link></p><p className="muted">Your account is stored locally in this browser. No MongoDB is used.</p></section>}
