import{useState}from'react';import{Link}from'react-router-dom';
export default function Forgot(){const[e,setE]=useState(''),[m,setM]=useState('');function submit(x){x.preventDefault();const users=JSON.parse(localStorage.getItem('pizza_users')||'[]');const found=users.find(u=>u.email.toLowerCase()===e.trim().toLowerCase());setM(found?'Demo mode: your account exists. Use Reset Password to set a new password.':'No account was found for this email.')}return <section className="card auth"><h1>Forgot Password</h1><form onSubmit={submit}><input type="email" value={e} onChange={x=>setE(x.target.value)} placeholder="Email" required/><button>Check Account</button></form><p>{m}</p><Link to="/login">Back to login</Link></section>}
