import {useState} from 'react';
const initial=[['Thin Crust','base',20],['Classic Hand Tossed','base',18],['Tomato Basil','sauce',25],['Mozzarella','cheese',30],['Onion','vegetable',40]];
export default function Admin(){const[inv,setInv]=useState(initial.map((x,i)=>({id:i,name:x[0],category:x[1],stock:x[2],threshold:10}))),[msg,setMsg]=useState('');return <><div className="hero adminhero"><div><p className="eyebrow">DEMO CONTROL PANEL</p><h1>Operations Dashboard</h1><p>Inventory changes are temporary and reset when the page is refreshed.</p></div></div><section className="grid"><div className="card"><h2>Temporary Inventory</h2>{inv.map(i=><div className="inv" key={i.id}><div><b>{i.name}</b><small>{i.category}</small></div><input type="number" value={i.stock} onChange={e=>setInv(inv.map(x=>x.id===i.id?{...x,stock:Number(e.target.value)}:x))}/><input type="number" value={i.threshold} onChange={e=>setInv(inv.map(x=>x.id===i.id?{...x,threshold:Number(e.target.value)}:x))}/><button onClick={()=>setMsg('Temporary inventory updated successfully!')}>Save</button></div>)}{msg&&<p className="success">{msg}</p>}</div><div className="card"><h2>Project Information</h2><p><b>Mode:</b> React-only</p><p><b>Database:</b> Not required</p><p><b>Backend:</b> Not required</p><p><b>Login:</b> Temporary variables in the React code</p><p className="muted">This version is designed to run with only npm install and npm run dev.</p></div></section></>}
