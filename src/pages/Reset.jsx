import{useState}from'react';import{Link}from'react-router-dom';
export default function Reset(){const[e,setE]=useState(''),[p,setP]=useState(''),[m,setM]=useState('');function submit(x){x.preventDefault();const users=JSON.parse(localStorage.getItem('pizza_users')||'[]');const i=users.findIndex(u=>u.email.toLowerCase()===e.trim().toLowerCase());if(i<0){setM('Account not found.');return}if(p.length<6){setM('Password must be at least 6 characters.');return}users[i].password=p;localStorage.setItem('pizza_users',JSON.stringify(users));setM('Password updated successfully.')}return <section className="card auth"><h1>Reset Password</h1><form onSubmit={submit}><input type="email" placeholder="Email" value={e} onChange={x=>setE(x.target.value)} required/><input type="password" placeholder="New password" value={p} onChange={x=>setP(x.target.value)} required/><button>Reset Password</button></form><p>{m}</p><Link to="/login">Back to login</Link></section>}
