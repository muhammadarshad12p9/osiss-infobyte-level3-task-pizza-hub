import {Link,Routes,Route,Navigate,useLocation} from 'react-router-dom';
import {useAuth} from './context/AuthContext';
import Login from './pages/Login';import Register from './pages/Register';import Forgot from './pages/Forgot';import Reset from './pages/Reset';import Dashboard from './pages/Dashboard';import Admin from './pages/Admin';
function Guard({children}){const{user}=useAuth();return user?children:<Navigate to="/login"/>}
function AdminGuard({children}){const{user}=useAuth();return user?.role==='admin'?children:<Navigate to="/dashboard"/>}
export default function App(){const{user,logout}=useAuth();const location=useLocation();const authPage=['/login','/register','/forgot','/reset'].includes(location.pathname);return <><header className={authPage?'simple-header':''}><Link className="brand" to={user?'/dashboard':'/login'}><span>🍕</span> PizzaHub</Link>{!authPage&&<div className="delivery-location">📍 <span>Deliver to</span><b> Your location</b></div>}<nav>{user?<><Link to="/dashboard">Menu</Link>{user.role==='admin'&&<Link to="/admin">Admin</Link>}<span className="user-chip">👋 {user.name}</span><button className="logout-btn" onClick={logout}>Logout</button></>:<Link className="nav-login" to="/login">Login</Link>}</nav></header><main><Routes><Route path="/" element={<Navigate to={user?'/dashboard':'/login'}/>}/><Route path="/login" element={<Login/>}/><Route path="/register" element={<Register/>}/><Route path="/forgot" element={<Forgot/>}/><Route path="/reset" element={<Reset/>}/><Route path="/dashboard" element={<Guard><Dashboard/></Guard>}/><Route path="/admin" element={<AdminGuard><Admin/></AdminGuard>}/><Route path="*" element={<Navigate to="/"/>}/></Routes></main><footer><b>🍕 PizzaHub</b><span>Fresh food. Fast delivery. Great taste.</span><span>© 2026 PizzaHub</span></footer></>}
